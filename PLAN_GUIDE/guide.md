
# 1. Create the virtual environment named 'thesis_env'
python3 -m venv thesis_env

# 2. Activate the environment
source thesis_env/bin/activate

# 3. Install dependencies
pip install -r requirements.txt